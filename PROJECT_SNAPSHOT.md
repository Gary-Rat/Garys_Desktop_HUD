# 项目快照

- 项目名：`desktop-hud`
- 技术栈：`Vite + React + TypeScript + Electron`
- 主界面：`src/App.tsx`
- 主样式：`src/App.css`
- 桌面能力入口：`electron/main.mjs`、`electron/preload.cjs`
- 类型桥接：`src/electron.d.ts`
- 桌面数据与分组规则：`src/desktopData.ts`

## 当前产品方向

- 这不是传统桌面整理器，而是一个常驻型透明桌面 HUD。
- 核心目标是把“桌面入口、搜索、分组、轻整理、状态控制”做成一套可伴随使用的覆盖层。
- 当前主路线已经从“贴近桌面”切走，改为“HUD 显示 / 隐藏”模式。
- 用户更看重的是轻量、自然、顺手，而不是复杂的自动整理。

## 当前界面结构

- 顶部：品牌、搜索、清空搜索、导入路径、设置入口。
- 左侧：分组栏，可切换当前主画布分组。
- 中间：单分组主画布，显示当前分组或搜索结果。
- 右上：`HUD` 小窗、`日程` 小窗。
- 中间弹窗：`设置`。
- 右侧：速览卡。

## 已完成的关键能力

### 1. 真实桌面读取

- Electron 已接入桌面读取。
- 能读取桌面一级项目、图标、类型、快捷方式解析路径。
- 支持打开项目、资源管理器定位、快捷方式定位到真实目标目录。

### 2. 分组与搜索

- 搜索已独立于分组展开状态。
- 主区不再并列展示所有分组，而是只展示一个当前分组主画布。
- 左侧分组栏点击时，主画布会先收起再展开，避免硬切。

### 3. 应用卡片与固定

- 画布内应用卡已收紧为固定尺寸，长名称单行省略。
- 右键菜单支持：`速览`、`定位`、`固定/取消固定`、`手动分类`。
- 固定后的卡片支持拖动换位。
- 固定卡可以插入普通卡之间，布局顺序会本地保存。

### 4. 设置 / HUD / 日程

- `设置` 为中间弹窗，并与分组主画布、分组栏、子窗口互斥。
- `HUD` 与 `日程` 为右上小窗，二者互斥展开。
- `设置` 已改成近乎不透明的独立面板，不再跟随 HUD 透明度。

### 5. HUD 显示隐藏

- 原“贴近桌面”功能已从前台交互中移除。
- 现在改为 `隐藏 HUD / 显示 HUD`。
- 设置中有开关，快捷键为 `Ctrl+Shift+Space`。
- 隐藏后窗口进入点击穿透，便于继续操作桌面。

### 6. 导入路径

- 除桌面外，现已支持额外导入自定义文件夹路径。
- 扫描结果会与桌面内容合并进同一套 HUD。
- 顶部已有 `导入路径` 入口。
- 设置里保留路径管理区。
- 当前用户希望进一步把它做成“按钮下的子目录式管理面板”。

### 7. 画布分页

- 画布区不再只截断显示前一部分卡片。
- 现在已支持分页展示。
- 支持左右翻页按钮。
- 支持在画布区域内用滚轮切页。
- 切换分组或搜索时，页码会自动回到第一页。

### 8. 滚动条与选字体验

- 滚动条已改为平时隐藏、滚动时短暂显示。
- 全局已禁止 HUD 非输入区文本选中。
- 输入框仍可正常选中文字。

## 当前仍成立的交互原则

- 设置打开时，不应再看到分组主画布、左侧分组栏或分组子窗口。
- 右键和子菜单应尽量轻，不要演变成重型管理面板。
- 卡片尺寸应稳定，后续主要做主题风格、颜色、材质自定义，而不是频繁改尺寸。
- 桌面仍是默认主来源，但不应成为唯一来源。

## 最近一轮真实改动摘要

- 把“贴近桌面”替换成了“隐藏 HUD”。
- 新增了额外路径扫描能力，Electron 端支持传入路径数组读取。
- 前端已接上导入路径状态、本地持久化和文件夹选择器。
- 卡片改为固定大小，文字省略。
- 分页展示替代了简单截断。
- 固定卡拖拽升级为可插入普通卡之间。
- 分组切换加入了过渡节奏。

## 当前已知待继续优化点

### 1. 导入路径入口形态

- 当前已经能导入和删除路径。
- 但用户希望它变成“导入路径按钮下的子目录”，不只是普通按钮。
- 目标是：点开后直接管理路径列表、添加、删除。

### 2. 主题风格自定义

- 用户后续更看重主题色、风格、材质自定义。
- 尺寸先稳定，后面主要开放颜色、面板风格、描边、阴影、氛围感。

### 3. 来源感知

- 当前不同来源路径的内容会合并显示。
- 后续可以考虑给卡片加“来源短标记”或“来源过滤”，避免多个导入目录混在一起时失去辨识。

## 下一个窗口建议优先顺序

1. 把 `导入路径` 做成真正的子目录式面板。
2. 给导入路径支持自定义短名。
3. 再考虑主题风格系统。

## 协作约束

- 默认只基于这份 `PROJECT_SNAPSHOT.md` 和用户明确点名的文件继续工作。
- 除非用户明确要求，否则不要全项目重扫。
- UI 微调优先只看：
  - `src/App.tsx`
  - `src/App.css`
- 桌面能力相关优先只看：
  - `electron/main.mjs`
  - `electron/preload.cjs`
  - `src/electron.d.ts`

## 2026-06-30 Hope List 与状态灯

- 右下角新增常驻 `Hope List`，支持记录迭代想法、固定分类（功能 / 体验 / 修复 / 灵感）、选择条目、删除条目，并持久化到 `desktop-hud:preferences`。
- `Hope List` 可调用 DeepSeek 生成计划性提示词；API Key 保存在 Electron `userData/deepseek-settings.json`，不进入 localStorage。
- DeepSeek 请求由 `electron/main.mjs` 代理，前端通过 `electron/preload.cjs` 和 `src/electron.d.ts` 暴露的方法调用。
- `Hope List` 下方新增状态便签板：本机时间 09:00-12:00、14:00-18:00 显示红灯，其余显示绿灯。
- Electron 全局 `Escape` 显示 HUD 绑定已移除，HUD 显示 / 隐藏只保留 `Ctrl+Shift+Space`；React 内部 Escape 仍用于关闭前端弹层。
# 2026-06-30 日程画布滚动与删除修正

- 时间轴日程点改为分层胶囊，40 分钟内的相邻日程会自动错层，减少重叠拥挤。
- 时间轴日程点新增独立删除按钮，点击时间仍切换状态，点击 `x` 删除该日程。
- 清单视图移到新增表单上方，切换清单时先看到全天小时目录。
- 日程面板内部新增自己的纵向滚动，不再依赖外层窗口裁切。

# 2026-06-30 日程全时段修正

- 日程时间轴从局部时段改为 `00:00` 到 `24:00` 全天视图，横向滚动查看完整一天。
- 清单标签页改为 24 小时目录，空时段也显示占位，不再只列已有日程。
- 日程查看标签、提醒状态、表盘时/分、添加按钮等新中文标签改用 Unicode 转义，避免编码链导致显示乱码。

# 2026-06-30 日程标签页与时间表盘

- 日程小窗新增查看标签页：`时间轴` / `清单`，当前标签保存在 `desktop-hud:preferences.agendaViewMode`。
- 添加日程时不再依赖可见手输时间框，改为小时/分钟滚动表盘；滚轮调节对应列，点击数字也可选择，提交仍保存标准 `HH:mm`。
- 新增日程提交后，时间表盘会重置到当前时间之后的下一个 15 分钟档。

# 2026-06-30 窗口层固定与日程提醒

- 设置内新增“窗口层固定”：Electron 主进程通过 PowerShell / Win32 枚举当前可见窗口，前端可从当前运行程序列表里选择窗口，调用 `SetWindowPos(HWND_TOPMOST / HWND_NOTOPMOST)` 固定或取消固定到桌面上一层。
- 已固定窗口保存到 `desktop-hud:preferences.pinnedWindows`，设置面板内可自由添加、移除、刷新当前运行窗口列表。
- 日程小窗加入横向滚动时间轴、当前时间标记、日程点位按钮；纵向滚轮在时间轴上会转成横向滚动。
- 日程输入槽被提升为更醒目的主输入区，时间输入保持轻量。
- 新增“日程到时提醒”开关，默认开启；到点后按“日期 + 日程 id + 时间”去重，触发系统通知和一段 WebAudio 提示音。
- 全局默认文字色从接近纯白改为更深的墨色，后续 UI 应继续避免纯白或浅色字体。
